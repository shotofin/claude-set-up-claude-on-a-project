CLAUDE.md defines general project details shared via github. it includes architecture, commands and conventions. it does not include secrete information or local preferences

These are the permissiions added. Tests can be run without interaction, pushing to github should be approved, access to local secret information or forsing a git push is forbidden

Verified: /memory shows CLAUDE.md loaded, and /permissions shows the allow/ask/deny rules configured.